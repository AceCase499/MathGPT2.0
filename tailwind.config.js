export const content = [
  "./app/**/*.{js,ts,jsx,tsx,mdx}",// '**/*' means apply css to all files AND subdirectories within this directory
];
export const theme = {
  extend: {},
};
export const plugins = [];
